{
  "name": "icmbais",
  "version": "1.0.0",
  "description": "web for a cause",
  "main": "index.js",
  "scripts": {
    "sass:build": "sass --no-source-map scss:css",
    "sass:watch": "sass --no-source-map --watch scss:css"
  },
  "author": "scribe",
  "license": "ISC",
  "dependencies": {
    "sass": "^1.72.0"
  }
}
